class A {

    method lt() : Unit = {
        var coins : Int[];
        var values : Int[];
        coins = new Int[10];

        values = new Int[coins.length];
      }
}

method main() : Unit = {
    println("main");
}