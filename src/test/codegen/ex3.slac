class A {
    var lt : Int;

    method lt() : String = {
        var a : String;
        var b : String;

        lt = 3;
        if (true) {
            b = "Whatever homie";
        } else {
            b = strOf(lt);
        };

        b
      }
}

method main() : Unit = {
    println("main");
}